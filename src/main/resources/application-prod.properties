
# Value: (paste your production Access Token here)
# Production overrides - do NOT enable schema auto-creation in production
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false

# --- Timezone: force Peru (Lima) ---
spring.jackson.time-zone=America/Lima
spring.jackson.serialization.write-dates-as-timestamps=false
spring.jpa.properties.hibernate.jdbc.time_zone=America/Lima
# In production, datasource should be provided via environment variables or external config
# Example (do not hardcode credentials here):
# spring.datasource.url=${SPRING_DATASOURCE_URL}
# spring.datasource.username=${SPRING_DATASOURCE_USERNAME}
# spring.datasource.password=${SPRING_DATASOURCE_PASSWORD}
